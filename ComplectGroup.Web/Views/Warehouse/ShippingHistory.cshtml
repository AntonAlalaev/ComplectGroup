@model List<ShippingTransactionDto>

@{
    ViewData["Title"] = "История отгрузок";
}

<div class="container mt-4">
    <h1>📋 История отгрузок товара</h1>

    @if (Model?.Any() == true)
    {
        <table class="table table-striped table-hover mt-3">
            <thead class="table-dark">
                <tr>
                    <th>Дата</th>
                    <th>Деталь</th>
                    <th class="text-center">Позиция</th>
                    <th class="text-center">Количество</th>
                    <th>Примечания</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var transaction in Model.OrderByDescending(t => t.ShippingDate))
                {
                    <tr>
                        <td>@transaction.ShippingDate.ToString("dd.MM.yyyy HH:mm")</td>
                        <td>[@transaction.Part.Chapter.Name] @transaction.Part.Name</td>
                        <td class="text-center">
                            <span class="badge bg-info">№@transaction.PositionId</span>
                        </td>
                        <td class="text-center"><span class="badge bg-primary">@transaction.Quantity</span></td>
                        <td>@transaction.Notes</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info mt-3">Нет записей об отгрузке</div>
    }

    <a asp-action="Index" class="btn btn-secondary mt-3">Назад на склад</a>
</div>
